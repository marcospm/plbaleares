{% extends 'base.html.twig' %}

{% block title %}Editar Sesión{% endblock %}

{% block body %}
<style>
    @media (max-width: 768px) {
        h1 {
            font-size: 1.5rem;
        }
        .card {
            margin-bottom: 1rem;
        }
        .form-control {
            font-size: 0.9rem;
        }
    }
</style>

<h1>Editar Sesión</h1>

{% if tipo == 'general' %}
    {{ include('sesion/_form_general.html.twig', {'button_label': 'Actualizar Sesión'}) }}
{% elseif tipo == 'municipal' %}
    {{ include('sesion/_form_municipal.html.twig', {'button_label': 'Actualizar Sesión'}) }}
{% else %}
    {{ include('sesion/_form.html.twig', {'button_label': 'Actualizar Sesión'}) }}
{% endif %}

<div style="margin-top: 2rem;">
    {{ include('sesion/_delete_form.html.twig') }}
</div>

{% endblock %}
