<form method="post" action="{{ path('app_recurso_delete', {'id': recurso.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recurso.id) }}">
    <button class="btn">Delete</button>
</form>
