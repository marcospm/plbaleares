{% extends 'base.html.twig' %}

{% block title %}Nuevo Examen Semanal{% endblock %}

{% block body %}
<div class="card">
    <div class="card-header">
        <h1>Nuevo Examen Semanal</h1>
    </div>
    <div class="card-body">
        <div class="alert alert-info" style="margin-bottom: 2rem;">
            <strong>💡 Información:</strong> Puedes crear un examen del temario general, un examen municipal, o ambos a la vez. 
            Si seleccionas temas del temario general Y un municipio con temas municipales, se crearán dos exámenes separados.
        </div>

        {{ form_start(form) }}
            {{ form_row(form.nombre) }}
            {{ form_row(form.descripcion) }}
            {{ form_row(form.fechaApertura) }}
            {{ form_row(form.fechaCierre) }}
            {{ form_row(form.dificultad) }}
            
            <div class="card" style="background-color: #f8f9fa; margin-bottom: 1.5rem;">
                <div class="card-body">
                    <h4 style="margin-top: 0; color: #4CAF50;">📚 Examen del Temario General (Opcional)</h4>
                    {{ form_row(form.temas) }}
                </div>
            </div>

            <div class="card" style="background-color: #e3f2fd; margin-bottom: 1.5rem;">
                <div class="card-body">
                    <h4 style="margin-top: 0; color: #2196F3;">🏛️ Examen Municipal (Opcional)</h4>
                    {{ form_row(form.municipio) }}
                    {{ form_row(form.temasMunicipales) }}
                </div>
            </div>
            
            <div class="d-flex gap-2 mt-4">
                <button type="submit" class="btn btn-primary">Crear Examen(es) Semanal(es)</button>
                <a href="{{ path('app_examen_semanal_index') }}" class="btn btn-secondary">Cancelar</a>
            </div>
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}

