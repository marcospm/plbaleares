doctrine:
  dbal:
    url: '%env(resolve:DATABASE_URL)%'
    server_version: 'mariadb-11.4.0'
    charset: utf8mb4
    default_table_options:
      charset: utf8mb4
      collate: utf8mb4_unicode_ci

    profiling_collect_backtrace: '%kernel.debug%'

  orm:
    validate_xml_mapping: true
    naming_strategy: doctrine.orm.naming_strategy.underscore_number_aware

    mappings:
      App:
        type: attribute
        is_bundle: false
        dir: '%kernel.project_dir%/src/Entity'
        prefix: 'App\Entity'
        alias: App

    controller_resolver:
      auto_mapping: false

when@test:
  doctrine:
    dbal:
      dbname_suffix: '_test%env(default::TEST_TOKEN)%'

when@prod:
  doctrine:
    orm:
      query_cache_driver:
        type: pool
        pool: doctrine.system_cache_pool
      result_cache_driver:
        type: pool
        pool: doctrine.result_cache_pool

  framework:
    cache:
      pools:
        # Doctrine cache pools - usar Redis en producción para mejor rendimiento
        # Durante el build, REDIS_URL puede no estar disponible, por lo que usamos un valor por defecto
        # En runtime, REDIS_URL se configurará automáticamente desde PLATFORM_RELATIONSHIPS
        # El valor por defecto 'redis://localhost:6379' solo se usa durante el build, no en runtime
        doctrine.result_cache_pool:
          adapter: cache.adapter.redis
          provider: '%env(default:REDIS_URL:redis://localhost:6379)%'
        doctrine.system_cache_pool:
          adapter: cache.adapter.redis
          provider: '%env(default:REDIS_URL:redis://localhost:6379)%'
