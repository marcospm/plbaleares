framework:
    cache:
        # Unique name of your app: used to compute stable namespaces for cache keys.
        #prefix_seed: your_vendor_name/app_name

        # The "app" cache stores to the filesystem by default.
        # The data in this cache should persist between deploys.
        # En producción, si REDIS_URL está disponible, se usará Redis (ver config/packages/prod/cache.yaml)

        # Namespaced pools use the above "app" backend by default
        pools:
            # Pools de caché - por defecto usan filesystem (compatible con build y local)
            # En producción, si REDIS_URL está disponible, se usarán Redis (ver config/packages/prod/cache.yaml)
            cache.partidas:
                adapter: cache.adapter.filesystem
                public: true  # Hacer público para poder acceder desde el contenedor
            cache.boe:
                adapter: cache.adapter.filesystem
                public: true  # Hacer público para poder acceder desde el contenedor
            cache.queries:
                adapter: cache.adapter.filesystem
                default_lifetime: 3600  # 1 hora para consultas comunes (leyes, temas, convocatorias)
                public: true  # Hacer público para poder acceder desde el contenedor
            # Pool principal de caché
            cache.redis:
                adapter: cache.adapter.filesystem
                default_lifetime: 3600
                public: true  # Hacer público para poder acceder desde el contenedor